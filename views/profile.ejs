<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
           <div class="div p-10 text-white">
            <h1 class="text-5xl mt-5">Hello, <%=user.username%></h1>
            <div class="post mt-5">
                <h3 class="">you can create your post</h3>
                <form action="/post" method="post">
                    <input class="w-1/3 bg-zinc-800 px-5 py-6 rounded-md mt-1" type="text" placeholder="What's on your mind?" name="content">
                    <br>
                    <input class="px-5 py-2 bg-blue-600 rounded-md mt-2" type="submit" value="Create New Post">
                </form>
            </div>
            <h1 class="mt-20">Your Posts</h1>
           </div>
            <div class="prt2 px-10 py-2 text-white flex flex-wrap gap-10 ">
               <% if(user.posts.length>0){%>
                <% user.posts.forEach(function(post){%>
                    <div class="task min-w-72 bg-zinc-600 rounded-md">
                        <h1 class="px-5 py-2 text-2xl"><%=user.username%></h1>
                        <p class="mt-2 px-5"><%=post.content%></p>
                        <div class="div flex flex-between mt-5 ">
                            <a class="inline-block  px-5 py-2 text-blue-600 text-xl" href="">like</a>
                            <a class="inline-block  px-5 py-2 text-yellow-600 text-xl" href="">edit</a>
                            <a class="inline-block  px-5 py-2 text-red-600 mt-1" href="">delete</a>
                        </div>     
                    </div>
                 <% }) %> 
               <% } else{%> 
                    <div class="task min-w-72 bg-zinc-600 rounded-md">
                        <p class="mt-2 px-5">No Post Yet...</p>
                    </div>
               <%}%>
            </div>            
    </div>
</body>
</html>